/**
 * District View -- barrel export.
 *
 * The DistrictViewOverlay is the main entry point. It self-gates
 * based on the morph state machine phase and renders the header,
 * dock, and content sub-components internally.
 *
 * @module district-view
 */

export { DistrictViewOverlay } from './district-view-overlay'
